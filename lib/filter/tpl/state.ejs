(function() {
    'use strict'

    angular.module('<%= module %>')
        .config(State);

    /* @ngInject */
    function State($stateProvider) {

        $stateProvider
            .state('<%= name %>', <%= JSON.stringify(content, null, '\t\t\t\t') %>);

    };

})();