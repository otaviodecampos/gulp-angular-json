(function() {
    'use strict'

    angular.module('<%= module %>')
        .constant('<%= name %>', Constant());

    function Constant() {
        return <%= JSON.stringify(contents, null, 10) %>;
    }

})();
