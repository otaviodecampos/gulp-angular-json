(function() {
    'use strict'

    angular.module('<%= name %>', [
        <% dependencies.forEach(function(dependencie, i) { %>
        '<%= dependencie %>'<% if(i != dependencies.length) { %>,<% } %> <% }); %>

    ]);

})();
